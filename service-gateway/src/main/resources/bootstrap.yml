server:
  port: 2200
spring:
  application:
    name: service-gateway
  cloud:
    config:
      profile: dev
      label: master
      username: webfuse
      password: webfuse
      fail-fast: true
      discovery:
        enabled: true
        service-id: central-config
##########################
## Eureka配置
##########################
eureka:
  client:
    service-url:
      defaultZone: http://webfuse:webfuse@localhost:2000/eureka/

