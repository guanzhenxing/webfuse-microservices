server:
  port: 8082
spring:
  application:
    name: test-customer-movies
  profiles:
    active: @env@
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://127.0.0.1:2201/
      username: zxguan
      password: zxguan
      profile: ${spring.profiles.active}
      label: master
