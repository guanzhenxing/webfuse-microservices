server:
  port: 8081
spring:
  application:
    name: test-provider-user
  profiles:
    active: @env@
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://127.0.0.1:2201/
      username: zxguan
      password: zxguan
      profile: ${spring.profiles.active}
      label: master
