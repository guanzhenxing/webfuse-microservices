spring:
  application:
    name: ms-service-registry
server:
  port: ${port}
eureka:
  client:
    service-url:
      defaultZone: ${defaultZone}
  instance:
#    hostname: ${hostname}
    instance-id: ${spring.cloud.client.ipAddress}:${port}