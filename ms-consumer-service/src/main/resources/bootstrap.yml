server:
  port: 8082
spring:
  application:
    name: ms-consumer-service
  cloud:
    config:
      profile: dev
      label: master
      username: webfuse
      password: webfuse
      fail-fast: true
      discovery:
        enabled: true
        service-id: central-config
##########################
## Eureka配置
##########################
eureka:
  client:
    service-url:
      defaultZone: http://webfuse:webfuse@localhost:2000/eureka/

